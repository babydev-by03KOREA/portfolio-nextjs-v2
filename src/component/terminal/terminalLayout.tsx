import Typist from "react-typist";

export default function TerminalLayout() {
    return (
        <Typist cursor={{ show: true }}>
            /BackEnd$ SpringBoot && NestJS && JSP <br/>
            /App$ Flutter && Swift && Kotlin <br/>
            /FrontEnd$ Next.JS && React <br/>
            /DB$ MongoDB && MySQL && PostgresQL && Firebase
        </Typist>
    );
}
